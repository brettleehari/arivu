<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- spine: C8 — no in-app themes; follows the system light/dark setting. The window background
         matches the Compose surface so there is no white flash in dark mode. -->
    <style name="Theme.Arivu" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>

# ios

Swift and SwiftUI app over `/core`, built in this session. See `leaves/engineering-ios.md`
for how to open and run it.

The core is shared and platform-free; everything here is the part that cannot be:
packaging, memory pressure, background execution, and the UI.
